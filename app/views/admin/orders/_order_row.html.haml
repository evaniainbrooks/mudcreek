%tr{data: { id: order.id }}
  - order_columns.each do |col|
    - if col.typed
      = render TableCellComponent.new(value: col.block.call(order), html_class: col.html_class)
    - else
      %td{class: col.html_class}= col.block.call(order)
