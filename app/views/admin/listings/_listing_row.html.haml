%tr{data: { id: listing.id }}
  - listing_columns.each do |col|
    - if col.typed
      = render TableCellComponent.new(value: col.block.call(listing), html_class: col.html_class)
    - else
      %td{class: col.html_class}= col.block.call(listing)
