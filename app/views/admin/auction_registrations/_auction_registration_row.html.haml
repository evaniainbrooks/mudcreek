%tr{data: { id: auction_registration.id }}
  - auction_registration_columns.each do |col|
    - if col.typed
      = render TableCellComponent.new(value: col.block.call(auction_registration), html_class: col.html_class)
    - else
      %td{class: col.html_class}= col.block.call(auction_registration)
