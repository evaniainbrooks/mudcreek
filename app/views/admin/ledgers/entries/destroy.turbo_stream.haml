= turbo_stream.replace "entries-section" do
  = render "admin/ledgers/entries_section",
      ledger: @ledger,
      entries: @entries,
      total_credits: @total_credits,
      total_debits: @total_debits,
      balance: @balance,
      entry: @new_entry
